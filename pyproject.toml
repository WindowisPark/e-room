[tool.poetry]
name = "ai-agent"
version = "0.1.0"
description = ""

[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
env_files = ".env.test"
